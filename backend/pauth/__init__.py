"""Local prior-authorization multi-agent console.

Package layout: FastAPI in `main`/`api`, LangGraph in `graph`, specialist
nodes in `agents`, and SQLite plus uploads under `config.settings.data_dir`.
"""
